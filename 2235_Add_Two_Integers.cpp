class Solution {
public:
    int sum(int num1, int num2) {
		return num1 + num2;
	}
	// In C, arr[index] is same as *(arr + index).
	// #define sum(X, Y) ((long)&((char*)(X))[Y])
	// by eric246871 on Leetcode
};